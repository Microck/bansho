---
phase: 03-authz-rate-limit
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/bansho/proxy/bansho_server.py
  - tests/test_security_pipeline.py
autonomous: true

must_haves:
  truths:
    - "Request pipeline applies auth -> authz -> rate limit in that order"
    - "Any failure stops the upstream call"
  artifacts:
    - path: "tests/test_security_pipeline.py"
      provides: "End-to-end security pipeline regression"
  key_links:
    - from: "tests/test_security_pipeline.py"
      to: "src/bansho/proxy/bansho_server.py"
      via: "exercise full request path"
      pattern: "tools/call"
---

<objective>
Wire the full security pipeline into the proxy and add an end-to-end regression test to prevent accidental bypass.

Purpose: Ensure deny-by-default enforcement cannot be accidentally skipped.
Output: Single E2E test covering auth, authz, rate limit ordering.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-authz-rate-limit/03-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure strict pipeline ordering in proxy</name>
  <files>src/bansho/proxy/bansho_server.py</files>
  <action>
Refactor the handler to make ordering explicit and auditable:
- `authenticate(request)` -> `authorize(tool)` -> `rate_limit(tool)` -> `forward_to_upstream()`.
- Ensure errors return appropriate status codes:
  - 401 auth missing/invalid
  - 403 authz denied
  - 429 rate limit exceeded
- Ensure denied/limited requests do not call upstream.
  </action>
  <verify>uv run pytest -q tests/test_security_pipeline.py</verify>
  <done>Proxy has a single, explicit pipeline and cannot forward on failure.</done>
</task>

<task type="auto">
  <name>Task 2: Add E2E regression test for bypass prevention</name>
  <files>tests/test_security_pipeline.py</files>
  <action>
Create a test that proves:
- Without key: 401 and upstream not called.
- With key but disallowed tool: 403 and upstream not called.
- With key and allowed tool but rate limited: 429 and upstream not called.
- With key + allowed + within limits: 200 and upstream called exactly once.
Use a fake upstream server that counts calls.
  </action>
  <verify>uv run pytest -q</verify>
  <done>One test covers the full security chain and asserts upstream non-invocation on failure.</done>
</task>

</tasks>

<verification>
- `uv run pytest -q` passes
</verification>

<success_criteria>
- Bansho enforces auth, authz, and rate limiting as a single non-bypassable pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/03-authz-rate-limit/03-04-SUMMARY.md`
</output>
