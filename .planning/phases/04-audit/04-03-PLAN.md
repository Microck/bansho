---
phase: 04-audit
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/mcp_sentinel/ui/dashboard.py
  - src/mcp_sentinel/ui/__init__.py
  - src/mcp_sentinel/main.py
autonomous: true

must_haves:
  truths:
    - "Operator can view recent audit events in a simple dashboard"
  artifacts:
    - path: "src/mcp_sentinel/ui/dashboard.py"
      provides: "HTTP page/API for recent audit events"
  key_links:
    - from: "src/mcp_sentinel/ui/dashboard.py"
      to: "src/mcp_sentinel/storage/postgres.py"
      via: "SELECT from audit_events"
      pattern: "FROM audit_events"
---

<objective>
Add a minimal dashboard to view recent audit events.

Purpose: Make audit logging visible and demo-friendly (AUDIT-04).
Output: Simple dashboard endpoint.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-audit/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement dashboard endpoint</name>
  <files>src/mcp_sentinel/ui/dashboard.py
src/mcp_sentinel/ui/__init__.py</files>
  <action>
Implement a small dashboard:
- Provide an HTTP endpoint (can be plain `http.server` or a tiny ASGI app) that lists the most recent N audit events.
- Include filters: by api_key_id and by tool_name.
- Default bind to localhost; require an admin API key to access the dashboard (reuse existing auth).
  </action>
  <verify>uv run python -c "print('dashboard imports')"</verify>
  <done>Dashboard module exists and can query recent audit events.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dashboard mode into entrypoint</name>
  <files>src/mcp_sentinel/main.py</files>
  <action>
Add `mcp_sentinel dashboard` subcommand:
- Starts the dashboard server.
- Reads `DASHBOARD_HOST` and `DASHBOARD_PORT` from Settings (add if needed).
- Ensure it shares Postgres config with the proxy.
  </action>
  <verify>uv run python -m mcp_sentinel dashboard --help</verify>
  <done>Dashboard can be started via CLI and runs without crashing.</done>
</task>

</tasks>

<verification>
- Start proxy, generate some traffic, start dashboard, confirm it shows the recent events
</verification>

<success_criteria>
- Recent audit events are viewable in a simple UI and access is protected.
</success_criteria>

<output>
After completion, create `.planning/phases/04-audit/04-03-SUMMARY.md`
</output>
