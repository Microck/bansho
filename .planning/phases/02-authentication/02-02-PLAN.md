---
phase: 02-authentication
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/bansho/middleware/auth.py
  - src/bansho/middleware/__init__.py
  - src/bansho/proxy/bansho_server.py
  - tests/test_auth_enforcement.py
autonomous: true

must_haves:
  truths:
    - "Requests without an API key are rejected with 401"
    - "API key can be provided via header or query param"
  artifacts:
    - path: "src/bansho/middleware/auth.py"
      provides: "Auth middleware producing authenticated context"
    - path: "tests/test_auth_enforcement.py"
      provides: "Auth enforcement regression test"
  key_links:
    - from: "src/bansho/proxy/bansho_server.py"
      to: "src/bansho/middleware/auth.py"
      via: "wrap tools/list and tools/call"
      pattern: "require_api_key"
---

<objective>
Enforce API key authentication at the Bansho boundary, rejecting unauthenticated MCP requests with clear errors.

Purpose: Turn the proxy into a security gateway (AUTH-01/03/04).
Output: Auth middleware wired into MCP tool methods + tests.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authentication/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement API key extraction + validation middleware</name>
  <files>src/bansho/middleware/auth.py
src/bansho/middleware/__init__.py</files>
  <action>
Create auth middleware functions:
- Accept API key from `Authorization: Bearer <key>` OR `X-API-Key: <key>` OR query param `?api_key=<key>`.
- Reject missing/invalid keys with 401.
- Return an `AuthContext` object containing `api_key_id` and `role` (no plaintext key).
- Never include the presented API key in error messages/logs.
  </action>
  <verify>uv run python -c "print('auth middleware imports')"</verify>
  <done>Middleware exists and can resolve a valid key into `{api_key_id, role}`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire auth enforcement into Bansho MCP handlers</name>
  <files>src/bansho/proxy/bansho_server.py</files>
  <action>
Update the MCP server implementation to enforce auth:
- On every MCP request that results in tool listing or tool invocation, require an `AuthContext`.
- Map auth failures to a consistent 401 response (MCP error envelope if applicable).
- Keep behavior explicit: no implicit allow-list; default deny unless authenticated.
  </action>
  <verify>uv run pytest -q tests/test_auth_enforcement.py</verify>
  <done>Unauthenticated list/call requests fail; authenticated requests succeed.</done>
</task>

<task type="auto">
  <name>Task 3: Auth enforcement tests (header + query)</name>
  <files>tests/test_auth_enforcement.py</files>
  <action>
Add tests proving AUTH-03/04:
- No key -> 401
- Header key -> 200 for `tools/list`
- Query param key -> 200 for `tools/list`
- Invalid key -> 401
  </action>
  <verify>uv run pytest -q</verify>
  <done>Tests cover missing/invalid/valid key paths for both header + query param.</done>
</task>

</tasks>

<verification>
- `uv run pytest -q` passes
- Manual: connect with an MCP client and confirm it must provide a key
</verification>

<success_criteria>
- Bansho rejects all unauthenticated MCP tool interactions by default.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-02-SUMMARY.md`
</output>
